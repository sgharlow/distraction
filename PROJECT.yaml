# PROJECT.yaml — machine-readable project contract. Single source of truth for volatile
# facts and gates; prose docs reference this file, they never restate its values.
# Standard: global CLAUDE.md "Documentation & Claim Discipline" (2026-07-01). Checked by /roadmap-lint.

name: distraction

# Authority resolved 2026-07-24 (7-23 lint flagged the vacuum: PLAN.md banners itself HISTORICAL).
# There is no forward plan doc — the product is live and in run-mode; the only open decision is the
# monetize-or-civic gate below. This file IS the plan. PLAN.md = historical build record;
# distraction-index-spec-v2.2.md = design reference; docs/superpowers/plans/* = executed build plans.
authoritative_plan: PROJECT.yaml

# customer-used = live at distractionindex.org with a real public audience (59+ weeks of data,
# newsletter subscribers, automated 4-hourly ingestion) — a FREE audience product; no revenue rung.
ladder: customer-used

derived:
  test_count: "grep -rE '\\b(it|test)\\(' src --include='*.test.*' 2>/dev/null | wc -l   # static proxy; true count via npx vitest run"
  head: "git rev-parse --short HEAD"
  branch: "git branch --show-current"

# The Q3 sunset review (2026-07-01) flagged this repo ZOMBIE-adjacent: a live product with real
# ongoing compute (ingestion every 4h) and NO revenue mechanism, no gate, no retire criterion.
# This gate is the counterweight — decide what the index is FOR, on a date.
gates:
  - id: monetize-or-civic
    metric: explicit_model_decision (premium API tier / pro subscription / deliberate free-civic with a cost cap / sunset)
    target: "decision recorded, with a monthly infra-cost ceiling attached  # ratified by Steve 2026-07-01"
    kill: "no decision by 2026-09-30 -> reduce ingestion cadence to daily + maintenance mode (cost floor)  # ratified 2026-07-01"
    owner: steve
    due: 2026-08-31

# Reliability posture, added 2026-07-31 after the July 2026 outage + blog-gap investigation.
# Recorded here because it is NOT derivable from the code: the three checks look like ordinary
# route handlers, and a future session could plausibly delete one as redundant. All three exist
# because a real failure ran undetected — do not remove without replacing the signal.
ops:
  dead_mans_switch: /api/monitor   # daily cron, CRON_SECRET-authed; emails via Resend (Supabase-independent by design, so it fires when Supabase IS the outage)
  health_endpoint: /api/health     # public; HTTP status driven by ingest freshness ONLY (stuck-week + missing-blog report in body, stay 200 — they are not outages)
  monitored_silent_failures:
    - ingest_freshness: "no article-bearing ingest inside the window — the July 2026 paused-Supabase outage, unnoticed 3 weeks"
    - stuck_live_week: "week left status='live' past its scheduled freeze; no retroactive week-level freeze exists, so nothing else signals it (week 2026-07-05)"
    - missing_blog: "frozen week whose cascade blog step never wrote — /api/freeze logs freeze_week() + pipeline_runs 'completed' BEFORE the cascade and swallows its errors, so this had NO signal at all for 17 weeks (67-83)"
  known_fragility:
    - "GDELT's DOC API applies a MULTI-HOUR burst cooldown (429 + ~11s latency) after a few dozen requests — not a per-5s rate limit, despite what its 429 body advises. Pacing does not defeat it (tested at 6s and 10s); only waiting does. scripts/backfill.ts --daily is therefore resumable: each day that returns 200 with >=40 articles is banked in scripts/.backfill-cache/<week>.json (gitignored) and re-runs request only the missing days, accumulating across cooldowns. It still refuses to freeze a week under 5/7 banked days rather than bake a distorted partial snapshot. A blocked backfill is the guard working, not a bug."
    - "Backfill must NOT be given a lower-volume substitute source to get past that guard. GNews' historical range call yields ~20 articles/week vs GDELT's ~1200 (fetchGNewsForDateRange runs 2 queries x max=10), and Google News RSS has no date parameters at all so it cannot backfill. A week thin enough to need them must refuse: every frozen week is built from GDELT by the same method, and that comparability is what makes the series meaningful. A guard that passes on thin data is worse than one that refuses."
    - "articles are idempotent (url UNIQUE + upsert ignoreDuplicates) but events is a plain .insert(). Re-processing a week duplicates every event and inflates compute_week_stats off the duplicates, permanently, in a frozen snapshot. backfill.ts hard-refuses any week that already has events; do not remove that check — the day cache makes re-runs routine."
    - "Never use date-fns format() to derive a week_id or a GDELT query window: it renders LOCAL time, so on a UTC-7 machine format(new Date('2026-07-19'),'yyyyMMdd') returns 20260718. This shifted every backfill day window one day early (~3-4% out-of-week articles) until fixed 2026-07-31. Use toISOString().slice(0,10) / the weekDayKeys() helper in src/lib/ingestion/day-cache.ts."
    - "The freeze cascade runs INSIDE /api/freeze at maxDuration=30s. Any model call it makes must fit that budget — Sonnet (~46-50s) does not and is killed uncatchably; the blog step uses Haiku (~20s) for this reason."

market:
  competitors_doc: docs/COMPETITORS.md   # not yet authored (Ground News, AllSides, Media Cloud — attention-analysis adjacents) — lint flags until it exists
  wtp_evidence: none   # audience exists (subscribers); paying intent never tested — the spec's Revenue Stream table is prose

monetization_path: none   # no billing/paywall code; the public v1 API + subscriber tooling are the natural surfaces if monetize-or-civic chooses monetize

post_event_plan: "n/a — no terminal event; the monetize-or-civic gate is the fork"
